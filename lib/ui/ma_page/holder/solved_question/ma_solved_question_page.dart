import 'package:flutter/material.dart';

class MaSolvedQuestionPage extends StatelessWidget {
  const MaSolvedQuestionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("모바일 지난 학습 페이지"),
      ),
    );
  }
}
