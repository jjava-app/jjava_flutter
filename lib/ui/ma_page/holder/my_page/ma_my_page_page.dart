import 'package:flutter/material.dart';

class MaMyPagePage extends StatelessWidget {
  const MaMyPagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("모바일 마이페이지"),
      ),
    );
  }
}
