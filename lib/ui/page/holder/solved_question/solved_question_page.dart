import 'package:flutter/material.dart';

class SolvedQuestionPage extends StatelessWidget {
  const SolvedQuestionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("지난 학습 페이지"),
      ),
    );
  }
}
